<%= form_for(@ntee) do |f| %>
  <% if @ntee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ntee.errors.count, "error") %> prohibited this ntee from being saved:</h2>

      <ul>
      <% @ntee.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br />
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
